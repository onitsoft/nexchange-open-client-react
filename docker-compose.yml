version: '2'
services:
    web:
        image: onitsoft/nginx:node
        volumes:
            - /nexchange/etc/letsencrypt:/etc/letsencrypt
            - /nexchange/etc/nginx/ssl:/etc/nginx/ssl
            - /nexchange/etc/nginx/nginx.conf:/etc/nginx/nginx.conf
            - /nexchange/etc/nginx/.htpasswd:/etc/nginx/.htpasswd
            - /nexchange/root:/nexchange/root
        ports:
            - "80:80"
            - "443:443"
        restart: always
    # react-client:
    #         image: onitsoft/nexchange-open-react-client
    #         volumes:
    #             - /deploy/app/build:/nexchange/root/nexchage-fed-proxy/nexchange
    # ico-page:
    #         image: onitsoft/ico-landing-page
    #         volumes:
    #             - /deploy/app/build:/nexchange/root/nexchage-fed-proxy/ico
    # fed-proxy:
    #         image: onitsoft/fed-proxy
    #         ports:
    #             - "80:80"
    #             - "443:443"

        
