const config = {
	API_BASE_URL: 'https://nexchange.io/en/api/v1',
	PRICE_FETCH_INTERVAL: 60000,
	ORDER_DETAILS_FETCH_INTERVAL: 10000,
	RECENT_ORDERS_INTERVAL: 10000,
	RECENT_ORDERS_COUNT: 11,
}

export default config;